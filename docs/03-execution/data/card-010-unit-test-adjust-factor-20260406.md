# CARD-010 单测补全：test_adjust_factor.py / 2026-04-06

## 目标

为 data 模块的复权因子计算逻辑补全单元测试：
1. 覆盖 gbbq 除权除息因子的核心计算路径
2. 覆盖各类权益事件（分红、送股、配股、合股）的因子计算
3. 覆盖边界条件（首日、无历史因子、连续多次权益事件）

## 前置条件

- [ ] card-001 完成：data 模块代码可用
- [ ] `src/lq/data/` 中复权因子相关代码已确定（函数签名稳定）

## 执行步骤

1. 审查 `src/lq/data/` 中涉及复权因子的代码，确认待测函数列表
2. 新建 `tests/unit/data/test_adjust_factor.py`
3. 编写测试用例：
   - **基础用例**：单次分红、单次送股、单次配股的因子计算
   - **复合用例**：同日分红 + 送股的因子叠加
   - **边界用例**：首日因子 = 1.0、无历史数据时的处理
   - **连续用例**：连续 3 次权益事件后的因子累乘链
   - **北交所用例**：BJ 市场代码映射后的因子获取
4. 确保所有测试用例使用固定输入数据（不依赖实际数据库）
5. 运行 `pytest tests/unit/data/test_adjust_factor.py -v`

## 验收标准

1. 新增测试用例 ≥ 10 条
2. 所有用例通过
3. 覆盖分红/送股/配股/合股四类权益事件
4. 覆盖至少 1 个边界条件
5. 不依赖外部数据库或网络

## 产出物

- `tests/unit/data/test_adjust_factor.py`（新建）
- evidence（pytest 输出截图）
- record + conclusion
